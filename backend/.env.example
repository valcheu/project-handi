# ====================================
# PROJECT HANDI - CONFIGURATION BACKEND
# ====================================

# Base de données PostgreSQL
# Les identifiants doivent correspondre à ceux définis dans docker-compose.yml
# Format : postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://val:val@localhost:5432/job_db"

# Clé secrète pour JWT (JSON Web Tokens)
# ⚠️ IMPORTANT : Changez cette valeur en production !
# Générez une clé sécurisée avec : openssl rand -base64 32
JWT_SECRET="dev_secret_key_change_in_production"

# Port du serveur backend
PORT=5000

# ====================================
# NOTES POUR LES DÉVELOPPEURS
# ====================================

# 1. Ce fichier est un EXEMPLE de configuration
# 2. Copiez ce fichier en .env : cp .env.example .env
# 3. Les valeurs par défaut fonctionnent avec docker-compose.yml
# 4. Ne committez JAMAIS votre fichier .env (il est dans .gitignore)

# ====================================
# CONFIGURATION DOCKER
# ====================================
# Si vous modifiez les identifiants dans docker-compose.yml,
# pensez à les mettre à jour ici également :
#
# docker-compose.yml :
#   POSTGRES_USER: val
#   POSTGRES_PASSWORD: val
#   POSTGRES_DB: job_db
#
# .env :
#   DATABASE_URL="postgresql://val:val@localhost:5432/job_db"
#                            ^^^ ^^^                  ^^^^^^^
#                            |   |                    |
#                            |   mot de passe        nom de la DB
#                            utilisateur

# ====================================
# DÉPANNAGE
# ====================================

# Si le port 5432 est déjà utilisé (PostgreSQL local installé) :
# 1. Modifiez docker-compose.yml : ports: ["5433:5432"]
# 2. Modifiez DATABASE_URL : postgresql://val:val@localhost:5433/job_db
#                                                            ^^^^

# Si vous ne pouvez pas vous connecter à la base de données :
# 1. Vérifiez que Docker Desktop est lancé
# 2. Vérifiez que le conteneur handi_db tourne : docker ps
# 3. Vérifiez que les identifiants correspondent à docker-compose.yml
